<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.piano.mapper.CommentMapper">

	<!-- 댓글 작성 -->
	<!-- CURRENT_TIMESTAMP vs. NOW() -->
	<insert id="commentWrite" parameterType="com.piano.vo.CommentVO">
		INSERT INTO COMMENT 
			(
				BOARD_SEQ, 
				BOARD_GRP_SEQ, 
				PARENT_COMMENT_SEQ, 
				REG_USER_SEQ, 
				COMMENT_TEXT, 				 
				REG_DT
			)
		VALUES
			(
				#{boardSeq}, 
				#{boardGrpSeq}, 
				#{parentCommentSeq}, 
				#{regUserSeq}, 
				#{commentText},  
				CURRENT_TIMESTAMP
			)
	</insert>


	<!-- 댓글 목록 -->
	<select id="getCommentList" parameterType="long" resultType="com.piano.vo.CommentVO">
		SELECT *
		FROM COMMENT
		WHERE BOARD_SEQ = #{boardSeq}
		AND DEL_YN = 'N'
		ORDER BY REG_DT ASC
	</select>
	
	
	<!-- 대댓글 작성 -->
	<insert id="insertReply" parameterType="com.piano.vo.CommentVO">
	INSERT INTO COMMENT 
		(
			BOARD_SEQ, 
			BOARD_GRP_SEQ, 
			PARENT_COMMENT_SEQ, 
			REG_USER_SEQ, 
			COMMENT_TEXT
		)
	VALUES 
		(
			#{boardSeq}, 
			#{boardGrpSeq}, 
			#{parentCommentSeq}, 
			#{regUserSeq}, 
			#{commentText}
		)
	</insert>
	

	<!-- 댓글 수정 -->
	<update id="updateComment" parameterType="com.piano.vo.CommentVO"> 
		UPDATE COMMENT
		SET COMMENT_TEXT = #{commentText},
			REG_DT = CURRENT_TIMESTAMP			    			
		WHERE COMMENT_SEQ = #{commentSeq} 	
	</update> 
	
	<!-- 댓글 삭제 -->
	<update id="deleteComment" parameterType="long"> 
		UPDATE COMMENT
		SET DEL_YN = 'Y'
		WHERE COMMENT_SEQ = #{commentSeq}
	</update>
		

</mapper>